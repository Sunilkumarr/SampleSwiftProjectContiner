# Uncomment the next line to define a global platform for your project
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'

target 'SampleSwiftProjectContiner' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for SampleSwiftProjectContiner
  
  pod 'Applozic', "~> 8.0.0"
  
  
  target 'SampleSwiftProjectContinerTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'SampleSwiftProjectContinerUITests' do
    # Pods for testing
  end
  
end
